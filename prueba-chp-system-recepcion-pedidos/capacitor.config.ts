import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.appixsoftware.recepcionpedidos',
  appName: 'prueba-chp-system-recepcion-pedidos',
  webDir: 'www'
};

export default config;
